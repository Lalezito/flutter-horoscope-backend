# Nixpacks config for native dependencies (sweph, canvas, sharp)
[phases.setup]
nixPkgs = ["python3", "gcc", "gnumake", "pkg-config", "cairo", "pango", "pixman", "libjpeg", "giflib", "librsvg"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]
